<?xml version="1.0" encoding="UTF-8"?>

<project name="map" default="do">

	<!-- import task -->
	<import file="${project.basedir}/../_4all/tasks.xml"/>

	<target name="do">

		<echo msg="**************************" />
		<echo msg="* MAP: babioon-cat       *" />
		<echo msg="**************************" />

		<phingcall target="symboliclink">
			<property name="source" value="${packagedir}/${projectname}/code/modules/mod_babioon_cat" />
			<property name="target" value="${targetdir}/modules/mod_babioon_cat" />
		</phingcall>

	</target>
</project>
